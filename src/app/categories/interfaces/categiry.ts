export interface Category  //do export for interface to enable import it in every where 
    {
  _id: string;
  name: string;
  slug: string;
  image: string;
  createdAt: string;
  updatedAt: string;
  title : string ;
  price : string ;
  description : string ;
}


